{
  "name": "astra",
  "private": true,
  "version": "1.0.0",
  "description": "astra — polygon space and planet simulation (Cloudflare Pages)",
  "scripts": {
    "dev": "npx serve -l 5500 .",
    "deploy": "npx wrangler pages deploy --project-name astra --branch main && echo \"Live: https://astra.pages.dev/\""
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
