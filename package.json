{
  "name": "astra",
  "private": true,
  "version": "1.0.0",
  "description": "astra — polygon space and planet simulation (Cloudflare Pages)",
  "scripts": {
    "dev": "npx serve -l 5500 .",
    "deploy": "bash scripts/cloudflare-deploy.sh",
    "deploy:pages-only": "CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN:?} npx wrangler pages deploy . --project-name astrabound-4ov --branch main",
    "domain:setup": "bash scripts/cloudflare-pages-domain.sh"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
